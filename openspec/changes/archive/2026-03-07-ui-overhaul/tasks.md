## 1. Global Styling & Design Tokens

- [x] 1.1 Define CSS variables for the new color palette (Background, Text, Accents) in `src/app/globals.css`.
- [x] 1.2 Implement the 8px spacing scale and typography tokens (font-weights, sizes) in `src/app/globals.css`.
- [x] 1.3 Update global body styles and common HTML elements (h1-h6, p) to use the new tokens.

## 2. Component Refactoring

- [x] 2.1 Update `src/components/SearchForm.tsx` to use new design tokens, high-contrast inputs, and professional button styles.
- [x] 2.2 Modernize `src/components/PermitList.tsx` with updated card layouts, subtle shadows, and improved visual hierarchy.
- [x] 2.3 Refactor `src/components/PermitDetailModal.tsx` and `PaymentModal.tsx` for consistent typography, padding, and sleek background colors.

## 3. Map & Overlay Modernization

- [x] 3.1 Update Mapbox marker colors (RED: #EF4444, YELLOW: #F59E0B, GREEN: #10B981) in `src/components/Map.tsx`.
- [x] 3.2 Adjust the 2-mile radius circle style (Indigo #6366F1 fill/stroke) in `src/components/Map.tsx`.
- [x] 3.3 Redesign the Map Legend and popup overlays to use the modern design system's background and text tokens.

## 4. Verification & Polish

- [x] 4.1 Verify color contrast and accessibility across all updated components using automated tools.
- [x] 4.2 Test responsive behavior on mobile and desktop viewports to ensure layout integrity.
- [x] 4.3 Perform a final visual audit to ensure the legacy beige aesthetic is completely replaced.
